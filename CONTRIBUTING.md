# Contributing

This repository is primarily an academic reproducibility package. Contributions should preserve reproducibility and avoid committing generated artifacts, credentials, or large model files.

Before opening a pull request:

1. Run `python scripts/validate_repository.py`.
2. Ensure notebook outputs are cleared.
3. Do not commit API tokens or local private paths.
4. Update documentation if paths, dependencies, or chromosomes change.
