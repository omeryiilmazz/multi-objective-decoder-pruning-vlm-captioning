# Results Directory

This directory contains lightweight result summaries that are safe to track with Git.

Current files:

- `chromosome_summary.csv`: consolidated low/mid/high chromosome summary across models.

Large raw outputs, generated captions, model checkpoints, or intermediate experiment artifacts should not be committed directly unless they are small and necessary for reproducibility.
